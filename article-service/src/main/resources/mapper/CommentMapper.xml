<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.spring.article.infrastructure.mybatis.mapper.CommentMapper">

    <insert id="insert">
        INSERT INTO comments (id, body, article_id, user_id, created_at)
        VALUES (#{comment.id}, #{comment.body}, #{comment.articleId}, #{comment.userId}, #{comment.createdAt})
    </insert>

    <select id="findById" resultType="io.spring.article.core.comment.Comment">
        SELECT id, body, article_id as articleId, user_id as userId, created_at as createdAt
        FROM comments
        WHERE article_id = #{articleId} AND id = #{id}
    </select>

    <delete id="delete">
        DELETE FROM comments WHERE id = #{id}
    </delete>
</mapper>
