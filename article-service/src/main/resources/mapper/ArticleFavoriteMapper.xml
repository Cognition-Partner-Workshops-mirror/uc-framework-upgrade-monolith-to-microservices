<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.spring.article.infrastructure.mybatis.mapper.ArticleFavoriteMapper">

    <insert id="insert">
        INSERT INTO article_favorites (article_id, user_id)
        VALUES (#{favorite.articleId}, #{favorite.userId})
    </insert>

    <select id="find" resultType="io.spring.article.core.favorite.ArticleFavorite">
        SELECT article_id as articleId, user_id as userId
        FROM article_favorites
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </select>

    <delete id="delete">
        DELETE FROM article_favorites WHERE article_id = #{articleId} AND user_id = #{userId}
    </delete>
</mapper>
